namespace Starlight.Enums.Features;

public enum FeatureStringValue
{
    /// <summary>
    /// Controls the default and fallback language code for Starlight's translation system
    /// </summary>
    DEFAULT_LANGUAGECODE=0
}